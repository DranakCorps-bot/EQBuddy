You are Opus / executor on EQBuddy Evolved. Work ONLY in this git worktree.

BASE: origin/main @ d8f88d66 (#337 SA-2 MERGED). Branch: claude/sa3-deadline-chips-20260905

TASK: **SA-3 only** from FABLE.md "### SA-3 - Net-new deadline chips..." Lane W. You own the screen.

## Soft posture from #337 sign
- #332 full-batch collision duty is **DISCHARGED** (85/0) — do **not** re-owe a full shoot.ps1 collision batch.
- **options-cards.png re-shoot still owed** — do it in this PR's shot work (or file clearly if blocked); not a merge gate for product if CI green, but prefer landing it here while you hold the screen.
- Do not start SA-4.

## SA-3 scope
1. Two new chip families on the SA-2 row: **Watch-fire** (when tracked rule fires / same event as AlertSoundPlan; linger short window or until dismissed) and **Buff expiring** (fixed T-minus from buff timer data Buffs already draws — verify snapshot in-PR).
2. Defaults = pinned constants; **no new Options rows** (mute = SA-4).
3. Do **not** retire `buffs` MiniStats key (SA-R / card cut later).
4. E2E facts + staged shot (rule-fire + expiring buff); predictions first (trap 23).
5. Build on HudChipRow from SA-2; HELM-FEEDBACK LIVE ASK **PREPEND**; UTF-8 only; never whole-file rewrite.

## Do NOT
- SA-4 / TEL / mojibake / Play Console / v1.99.19 / player door
- Merge yourself

When done: print PR URL + head SHA and exit.
