# Flake ledger

Intermittent failures we have already paid for. Soft / CI / last-look all write
here. **“Passed on rerun” is an observation, not a resolution.**

A row stays **open** until a disposition other than rerun exists: a guard, a
harness fix, a named environment exclusion, a lane that no longer exists, or
an explicit “will not fix — here is why.” Closing on a green retry is how a
real race becomes tribal knowledge and then a merge-blocker again.

## How to add a row

Append. Do not rewrite the table. One failure mode per row. Signature is the
thing a later seat can grep — test name, assert fragment, or log line — not a
novel.

| Column | Meaning |
|---|---|
| Signature | Stable grep key (test method, assert text, or log token) |
| Occurrences | Dates / run IDs / PR numbers. Add, do not replace. |
| Affected test | Class and method if known |
| Environment | `ci/build-and-test`, `ci/e2e-windows`, local Windows, local `check.ps1`, … |
| Disposition | `open` · `observed-rerun` · `guarded` · `harness` · `lane-gone` · `wont-fix` — plus one clause |

Required header (pinned by `DocumentationTests`):

`Signature | Occurrences | Affected test | Environment | Disposition`

## Ledger

| Signature | Occurrences | Affected test | Environment | Disposition |
|---|---|---|---|---|
| `TheLiveRoomAndTheKillsWindowAgreeAboutTheSessionsKills` — `kills` vs `shellLiveKillRows`, `Expected: 10 / Actual: -1`. **The tell that separates it from a late render is one line up:** the same key PASSED an `Assert.True(... > 0)` two lines earlier in the same test, so it was present and then absent. Grep `Actual: -1` on this test, not the number | 2026-09-20 `main` at `66fbed08` (the PR #727 merge), run `35503846320`, the only red in the suite with `build-and-test` green beside it. **The tree already contained DRA-225's fix** (`f992b150`, PR #725, three commits earlier), which is what makes this occurrence worth its own row rather than a line on one of the three DRA-225 closed | `ShellHostTests.TheLiveRoomAndTheKillsWindowAgreeAboutTheSessionsKills` (line 964) | `ci/e2e-windows` | **CLOSED 2026-09-20 by DRA-228.** **DRA-225 closed its three rows and did not close the FAMILY, and this is the row that says so.** Its fix was the harness's share mask, which removed the blanked-dump mode (103 of 378 writes denied, 27% → 0 of 377) and knowingly traded it for the reader seeing a PARTIAL file (223 in 102,503, 0.22%) — because `WidgetDump` published the dump with `File.WriteAllText`, which TRUNCATES before it fills. That residual is this red. **The fix is on the WRITER, once, rather than at the 485 assert-on-`DumpValue` call sites** (trap 4): `UI.Shared.WholeFilePublish` builds the next dump in a scratch file and replaces the published name atomically, so `debug.txt` always resolves to a COMPLETE dump and `-1` goes back to meaning what `AppHarness.DumpValue`'s docstring says it means. **Two wrong turns were measured out of it and both are recorded in the file**, because each looked right and neither was: a fall-back-to-plain-write on a refused replace ran **2,833 times in 4,000 publishes** and truncated every time, putting the original defect back at 71%; and a retry added to the READER for a refused open was deleted after instrumenting showed the open is refused **zero times in 1,986,753 reads** — an arm no test could redden is a claim, not code (trap 78). Guard is `WholeFilePublishTests` in **`EQBuddy.Tests`, so it runs in `build-and-test`** rather than only in the lane it stabilises. Prove-failed against three mutants: restoring the truncating write reddens with `torn reads: 134 of 183`, dropping the replace retry reddens the separate `publishes dropped to keep the previous file: 2034 of 4000` line (a publisher that goes quiet is the frozen-dump picture, and it would read as zero torn reads — trap 11), and the deleted reader retry reddened nothing, which is why it is not in the tree. Measured green: 4,000 publishes against a reading thread, **zero torn reads and zero sentinels in 52,683 reads**, every publish atomic. |
| `NoMoreThanTwoFetchesAreEverInFlight` | Helm last-looks 2026-09-04 (PR #292 / #293 era); “re-run, do not fix product” | `EqlWikiMobsTests.NoMoreThanTwoFetchesAreEverInFlight` | `ci/build-and-test` | **open** — observed-rerun only. Wiki concurrency; do not invent a product fix from one red. |
| `AForeignWriteBetweenLoadAndSaveIsReported` | Helm last-looks 2026-09-04 (PR #292); Mobile sounds #264 sign-off | `SettingsClobberTests.AForeignWriteBetweenLoadAndSaveIsReported` | `ci/build-and-test` | **open** — observed-rerun only. Timing around a file that another writer touched. |
| `SessionGoesLive` / `kills to reach N; last seen N+1` | E2E four rounds ~2026-09-04; runs `33924866579`, `33925423795`; trap 56 | `EndToEndTests` session ingest (`SessionGoesLive`, `KillThenLoot`) | `ci/e2e-windows` | **guarded** — two-moment dump. `PaintOneMoment` / `surfacesBehind` as assert / `tick` abort. A new equality sail-past is a **new** row, not this one closed-by-rerun. |
| `no visible window matching '…'` mid-batch, passes alone | #306 three runs (`shell-gear-narrow`, `options-window`, `drops-window`); trap 61 | `scripts/shoot.ps1` row varies | local Windows / second seat | **guarded** — screen lock in `shoot.ps1` and `AppHarness.Launch`; `ScreenLockTests`. A new random-row fail with the lock held is a **new** row. |
| `MapCircleMenuConfirmsThenRemovesTheSpawnPoint` thread-affinity cleanup | Helm 2026-09-04 (PR #277 era) | Avalonia ZoneWindowsRenderTests.MapCircleMenuConfirmsThenRemovesTheSpawnPoint (suite deleted with the lane; un-backticked so `DocumentationTests` can name it) | `ci` Avalonia Linux (deleted) | **lane-gone** — E-2c removed the Avalonia suite. Shape (shared session + parallel collections) is trap 57; do not re-learn it on a new shared-state project. |
| ThemeBodyCap / `ThemeBodyCap` flake on docs-only head | Helm 2026-09-04 (PR #276) | Avalonia theme-body cap render | `ci` Avalonia | **lane-gone** — same deletion. Do not treat a docs-only red as a docs defect. |
| `WorldWindow` one-off during docs-only last-look | PR #328 (trap 60 filing); Helm: “one WorldWindow flake; docs-only — do not expand” | E2E / shoot World host | `ci/e2e-windows` | **open** — observed-rerun. Docs-only tip; do not expand product. |
| Tooltip clock death (`EQBuddy stopped updating but I can still use it`) | #354 / #359 / #360; minidump `freeze-tick3` run `34075983046`; trap 63 | `ToolTipTimerTests` (E2E), `ToolTipPolicyTests` | player Windows; `ci/e2e-windows` | **guarded** — `ToolTipPolicy` 30 s + overflow arithmetic; prove-failed at zero ticks. Intermittent because the overflowed due time only wins when another timer is already late. |
| `TheShellAndTheWorldWindowAgreeAboutTheSameRoom` — `spawnsRows` vs `shellWorldSpawnsRows`, `Expected: 4 / Actual: 13` | 2026-09-09 PR #472, run `34371064898` (tip `9dff4997`). Same branch one commit earlier (`10555c87`, run `34370862597`) passed. **AGAIN 2026-09-15** on PR #631 (DRA-68), run `34932892258` at tip `041a74df`: the SAME numbers, `Expected: 4 / Actual: 13`, the only red in a suite where `build-and-test` was green, and 2 s. | `ShellHostTests.TheShellAndTheWorldWindowAgreeAboutTheSameRoom` (line 353) | `ci/e2e-windows` | **CLOSED 2026-09-20 by DRA-225** (was open — observed-rerun, twice). **The cause was the harness's own READ, and it is measured rather than inferred:** `AppHarness.DumpValue` read `debug.txt` with `File.ReadAllText`, whose `FileShare.Read` mask DENIES a concurrent writer — and the writer is the app. `WidgetDump.MaybeWrite` writes the dump inside a try whose catch replaces the whole file with `tick=… dumpError=…`, every other key absent, so a harness read that overlapped a dump write made the app blank its own dump for a whole tick. Two processes over one file at the dump's real size (~6.4 KB): with `FileShare.Read` the writer was denied **103 of 378** writes (27%); with `FileShare.ReadWrite \| Delete` it was denied **0 of 377**, at the cost of the reader seeing a partial file 223 times in 102,503 (0.22%) — a spoiled read of microseconds in place of a blanked dump of a second. **The `4 / 13` mode on this row is the OTHER half and needed the other half of the fix:** two hosts sampled at two moments while the replay was still landing, now taken in ONE `WaitForDumpValues` read that must carry all ten keys. `DumpValues` alone could not have closed the `-1` rows (one read of a `dumpError` dump answers -1 for every key at once) and `WaitForDump` could not have closed this one (it is an equality, and neither side of an agreement is known in advance). Prove-failed in `DumpReadTests`: restoring `File.ReadAllText` reddens with `Expected: 8 / Actual: -1`; dropping the all-keys-present check reddens with `Expected: [119, 119] / Actual: [119, -1]`.  **The run that closes this row is PR #725's `e2e-windows`, run `35498738024`** (405/405 in the hosted lane, 17 m 35 s, with `build-and-test` green beside it); the local full suite was 405/405 at 10 m 02 s and `scripts/check.ps1` all-green at 5,670 unit tests. A rerun green does not close a row — the applied fix plus its prove-fail does, and both are in that run. Kept below: **The tip that failed was channel-markdown-only** (`DECISIONS.md` / `FABLE-FEEDBACK.md` / `HELM-FEEDBACK.md`); it cannot change a spawn-row count, so this is the harness, not the product. Two hosts read the Camps room at different moments and the seeded spawns are still arriving — the trap 56 shape, one surface further out. Possibly the same mode as the docs-only `WorldWindow` row above; filed separately because that one names no test and this one does. **Do not expand product from this red.** **What the 2026-09-15 occurrence ADDS: the numbers did not move.** `4 / 13` six days apart, on two unrelated branches, is the same READ at the same two moments — not a count that drifted with the fixture, and not the `-1` missing-key mode of the two rows below. The DRA-68 diff is `README.md` / `EQBuddy-Evolved.md` / `docs/TestPlan.md` / `LandingSourceClaimsTests.cs` + two channel files with **`src/` untouched**, so for the second time this row is a red on a tree that provably cannot reach a shell room — which is evidence the fix belongs in the harness's read (`DumpValues` for one moment, or `WaitForDump` on `shellWorldSpawnsRows`) and not in Camps. Still NOT applied here: the E2E lane needs an interactive Windows session and the screen lock (trap 61), and a docs card is the wrong seat to guess a harness edit it cannot run. |
| `TheShellAndTheWorldWindowAgreeAboutTheSameRoom` AGAIN, one line down — `spawnsRows` vs `shellWorldSpawnsRows` (`Expected: 0 / Actual: -1`) and `spawnsZones` vs `shellWorldSpawnsZones` (`Expected: 119 / Actual: -1`) — the ABSENT-read mode. Both the key and the expected count move between occurrences while the sentinel does not, so grep `Actual: -1` on this test, not either number | 2026-09-13, local full `tests/EQBuddy.E2E` on `opus-dra71-d2` (DRA-71 D2), 348 tests, this the only red at 7 m 19 s. Re-run ALONE immediately after: passed in 2 s. **AGAIN 2026-09-19, and this time in CI** — PR #700 (DRA-207), run `35434300574` at tip `3d09b04b`: `spawnsZones` vs `shellWorldSpawnsZones`, `Expected: 119 / Actual: -1`, the only red in 382 tests at 2 s, with `build-and-test` green in the same run. | `ShellHostTests.TheShellAndTheWorldWindowAgreeAboutTheSameRoom` — line 355 in 2026-09-13, line 354 (the `spawnsZones` assert) in 2026-09-19; the line moves, the method does not | local Windows full suite; `ci/e2e-windows` 2026-09-19 | **CLOSED 2026-09-20 by DRA-225** (was open — observed-rerun, twice). Same cause and same fix as the row above, and this is the row whose evidence named it: the `-1` was never a value, it was the whole dump replaced by `WidgetDump`'s `tick=… dumpError=…` catch after the harness's `File.ReadAllText` denied the app its write (measured: 103 of 378 writes denied at 27%, versus 0 of 377 under the shared mask the harness uses now). The six trailing `DumpValue` moments this row correctly named are now ONE `WaitForDumpValues` read that must carry every key — which is why the KEY moving between occurrences (`spawnsRows`, then `spawnsZones`) was the right read of the evidence: the flake belonged to the read, not to any value behind it. Prove-failed against two mutants in `DumpReadTests`, one of which reproduces this row's signature exactly: `Expected: [119, 119] / Actual: [119, -1]`. **This row's own 2026-09-13 confound stays retired and stays admitted** — the 2026-09-19 CI sighting had no concurrent build, and the fix rests on the 2026-09-19 evidence, not the confounded one.  **The run that closes this row is PR #725's `e2e-windows`, run `35498738024`** (405/405 in the hosted lane, 17 m 35 s, with `build-and-test` green beside it); the local full suite was 405/405 at 10 m 02 s and `scripts/check.ps1` all-green at 5,670 unit tests. A rerun green does not close a row — the applied fix plus its prove-fail does, and both are in that run. Kept below: Same test and same shape as the 2026-09-09 row above; filed here because the NUMBERS are different in the way that matters: `-1` is `AppHarness.DumpValue`'s missing-key sentinel, so this is the ABSENT-read mode of the `EveryLandedRoomIsReachableByItsOwnAddress` row below rather than the two-hosts-disagree mode of the row above — the test waits on `shellWorldTab`/`worldTab` and then takes SIX further `DumpValue` reads, each its own moment (trap 56). **And this seat has a confound it will not hide:** `scripts/check.ps1` was started while the suite was still running and it BUILDS, so a swapped assembly under a live run cannot be excluded as the cause. That is this seat's own process error, not a property of the test, and the lesson is the row: never start a build beside a running E2E lane — the screen mutex (trap 61) covers the display and nothing covers `bin`. The diff under it touches `DesignSystem`, `HelperRoom` and `QuestsView` and no World surface. **Do not expand product from this red.** **What the 2026-09-19 occurrence ADDS, and it is the strongest evidence on this row yet:** (a) **it retires this row's own confound.** The 2026-09-13 sighting could not exclude a swapped assembly because a build was running beside the suite; 2026-09-19 is a clean hosted `ci/e2e-windows` lane with no concurrent build, so the ABSENT read reproduces with that explanation gone. (b) **The KEY moved.** Every prior sighting of this test was `spawnsRows`; this one is `spawnsZones`, one assert further down. That is the same argument the `EveryLandedRoomIsReachableByItsOwnAddress` row below makes from its moved ADDRESS — one key failing repeatedly would point at Camps, but two different keys failing the same late read says the flake belongs to the READ and not to any value behind it. Taken with the moved expected count (`0`, then `119`), the only invariant across occurrences is the `-1` sentinel itself. (c) **The tip provably cannot reach the product:** PR #700 is a single-file diff to `scripts/channel-rotate.py` (+306/-39), Python only, `src/` and `tests/` untouched — and `e2e-windows` was GREEN on its exact base commit `68da131e` (run `35431875549`) and on main's `eefcd1f5` (run `35434796840`). That is the third time this row has gone red on a tree that cannot address a shell room. **This is now evidence FOR the named-but-unapplied candidate fix** — collapse the six trailing `DumpValue` moments into one `DumpValues` read, or `WaitForDump` each key (trap 56). Still not applied here for the reason the rows above give: the E2E lane needs an interactive Windows session and the screen lock (trap 61), and a tooling card is the wrong seat to guess a harness edit it cannot run. |
| `EveryLandedRoomIsReachableByItsOwnAddress` — `shellRail`, `Expected: <rail count> / Actual: -1` (7 on 2026-09-09, 8 on 2026-09-14 and 2026-09-20 — the rail gained a row between the first two, so the EXPECTED number moves and the sentinel does not; grep the `-1`, not the count) | 2026-09-09 `main` at `511d221c` (the PR #472 merge), run `34373888681`. The NEXT commit on main, `4ec7c26c`, CONTAINS `511d221c` and its `e2e-windows` was green. **AGAIN 2026-09-14** on `main` at `104eab4b` (the PR #608 merge — DRA-74 M0-1), run `34855488655`: a DIFFERENT address, `world:spawns`, `Expected: 8 / Actual: -1`, the only red in the suite, 2 s. Every other address in the theory passed **including `world:drops`**, the one that failed in 2026-09-09. **AGAIN 2026-09-20** on PR #713 (DRA-216 plan correction), run `35481322450`: a THIRD address and the first one that is not a World room — `address: "gear:gear"`, `key: "shellGearTab"`, `Expected: 8 / Actual: -1`, the only red in the suite, 2 s, with `build-and-test` green in the same run. Its own sibling `gear:inventory` — same key, same second read — PASSED in that run, 11 s later. | `ShellHostTests.EveryLandedRoomIsReachableByItsOwnAddress` (line 317) — `address: "world:drops"` 2026-09-09, `address: "world:spawns"` 2026-09-14, `address: "gear:gear"` 2026-09-20; `key: "shellWorldTab"` for the first two, `key: "shellGearTab"` for the third | `ci/e2e-windows` | **CLOSED 2026-09-20 by DRA-225** (was open — observed-rerun, four times). **The four sightings' one invariant was right, and it pointed at the read rather than at any room:** the harness's `File.ReadAllText` denied the app its dump write (`FileShare.Read` versus a writer), `WidgetDump`'s catch then replaced the whole file with `tick=… dumpError=…`, and the SECOND bare `DumpValue` read -1 for whatever key it asked. That is why the address moved from `world:drops` to `world:spawns` to `gear:gear`, why the key moved from `shellWorldTab` to `shellGearTab`, why the expected count moved with the rail, and why `gear:inventory` passed 11 s later in a different process — none of those is the variable, the poisoned tick is. Measured: 103 of 378 writes denied (27%) under the old mask, 0 of 377 under `FileShare.ReadWrite \| Delete`. **The call site now takes `WaitForDump("shellRail", ShellPages.Landed.Count, …)` — an EQUALITY wait, because this one value IS known before the app is asked**, which is the one place in this card's three tests where `DumpValues` would have been the wrong tool. Prove-failed in `DumpReadTests`: restoring `File.ReadAllText` reddens `TheHarnessReadsTheDumpWhileTheAppHoldsItOpenForWriting` with `Expected: 8 / Actual: -1`, this row's signature verbatim.  **The run that closes this row is PR #725's `e2e-windows`, run `35498738024`** (405/405 in the hosted lane, 17 m 35 s, with `build-and-test` green beside it); the local full suite was 405/405 at 10 m 02 s and `scripts/check.ps1` all-green at 5,670 unit tests. A rerun green does not close a row — the applied fix plus its prove-fail does, and both are in that run. Kept below: **-1 is `AppHarness.DumpValue`'s missing-key sentinel, so the rail count was not WRONG, it was ABSENT from that read** — and a wrong rail count would have failed all 21 addresses, not the one. The test waits on `shellWorldTab` and then reads `shellRail` in a SECOND `DumpValue`: two reads, two moments, the trap 56 shape the harness already owns tools for (`DumpValues` for one read, `WaitForDump` for a value that may not have landed). Candidate fix NAMED, not applied — the E2E lane needs an interactive Windows session and the screen lock (trap 61), so this seat filed rather than guessed at a harness edit it could not run. **What the second occurrence ADDS is the reason it is worth a line rather than a shrug: the ADDRESS moved.** One `InlineData` row failing twice would point at that room; two different rooms failing the same second read says the flake belongs to the READ and not to Drops or Camps — which is evidence FOR the named candidate fix and against "`world:drops` is slow to land" as the explanation. It also arrived the same way twice: a **docs-only tip on `main`**, where the diff provably cannot reach a shell room. The 2026-09-14 tree left `src/` untouched and had passed `e2e-windows` **eight minutes earlier** on PR #608 (run `34853749930`) — same tree, opposite result, which is as close to a controlled pair as this lane produces. **Do not expand product from this red**; P1b touched no shell room, and DRA-74 touched no code at all. **What the 2026-09-20 occurrence ADDS, and it is the strongest evidence this row has: the flake left the World theme.** Every prior sighting was a `world:*` address behind `shellWorldTab`. This one is `gear:gear` behind `shellGearTab` — a different room, a different tab key, a different `DumpValue` — while **`gear:inventory`, which reads the SAME `shellGearTab` key through the SAME second read, passed 11 s later in the same run.** So the surviving explanation cannot be "the World rooms are slow to land", and it cannot be "`shellGearTab` was absent", because the sibling read it. What is invariant across all four sightings is one thing only: **the `-1` sentinel on the SECOND read.** That is the `DumpValues`/`WaitForDump` candidate fix (trap 56) named twice above, now with a third independent theory row behind it. The tip is again one that provably cannot reach a shell room — PR #713 is `docs/proposals/DRA-216-evolved-gear-helper-plan.md` +32/−1 with `src/` and `tests/` untouched, and `build-and-test` was green in the same run. That is the **fourth** red on a tree with no path to the product. **Still NOT applied here for the same reason as the rows above** — the E2E lane needs an interactive Windows session and the screen lock (trap 61), and a Planner docs card is the wrong seat to guess at a harness edit it cannot run. **This row has now earned its own card rather than another occurrence line: DRA-225**, which carries the fix, its prove-fail bar, and the two sibling `TheShellAndTheWorldWindowAgreeAboutTheSameRoom` rows that want the same pass. The next seat that can run the lane should take that card, not re-file the evidence. |
| `TheShellAndTheGearLootWindowAgreeAboutTheSameRoom` — `gearRows` vs `shellGearRows`, `Expected: 3 / Actual: -1`. The `-1` absent-read mode again, and **the first occurrence of it outside the World surface**; grep `Actual: -1` against `ShellHostTests`, not the count | 2026-09-19, PR #692 (DRA-179 D2), run `35414589898` at tip `fb8ab751`: the only red in 382 tests, 2 s, with `build-and-test` GREEN in the same run. | `ShellHostTests.TheShellAndTheGearLootWindowAgreeAboutTheSameRoom` — line 430, the third of six trailing `DumpValue` asserts | `ci/e2e-windows` | **open** — observed-rerun. Same mechanism as the two rows above and filed separately because it names a different test and a different ROOM: the test `WaitForDump`s `shellGearTab` and `gearLootTab`, then takes **six** further `DumpValue` reads, each its own moment (trap 56), and the third came back ABSENT. **What this occurrence ADDS is the scope of the mode.** Every prior sighting in this family read a World-room key (`spawnsRows`, `spawnsZones`) or the rail (`shellRail`); this is the Gear room and a fourth key, `shellGearRows`. One key failing repeatedly would point at Camps; four keys across three test methods and two rooms failing the same late read says the flake belongs to `DumpValue`'s trailing-read pattern and to nothing behind any of them — which is further evidence FOR the named-but-unapplied candidate fix (collapse the trailing `DumpValue` moments into one `DumpValues`, or `WaitForDump` each key). **The tip provably cannot reach a shell room:** PR #692 is docs plus one unit-test file — `CLAUDE.md`, `docs/ops/execution-flow.md`, `tests/EQBuddy.Tests/DocumentationTests.cs` and two channel ledgers — with `src/` and `tests/EQBuddy.E2E/` untouched, and `DocumentationTests` is a different assembly from `EQBuddy.E2E` entirely. **And the controlled pair is tighter than this lane usually gives:** CI run `35422595980` was SUCCESS — both gates — on PR #697's head, which is the exact commit this branch was rebased onto hours later. Still not applied here for the reason every sibling row gives: the E2E lane needs an interactive Windows session and the screen lock (trap 61), and a docs card is the wrong seat to guess a harness edit it cannot run. **Do not expand product from this red.** |

| `TheShellAndTheCreatureWindowAgreeAboutTheDropsTheyBothShow` — mid-suite only; **assert text NOT captured** (this seat's grep filtered it before it was read, which is the mistake to avoid next time) | 2026-09-09, local full `e2e-windows` run on `claude/opus-runes-and-next-card-20260909` (DRA-44 + DRA-36), 331 tests, this the only red at 6 m 39 s. Re-run ALONE immediately after: passed in 1 s. | `ShellHostTests.TheShellAndTheCreatureWindowAgreeAboutTheDropsTheyBothShow` | local Windows full suite | **open** — observed-rerun, and the rerun is an observation. Same family as the `no visible window matching` and shared-dump rows above: two hosts reading one dump at different moments (trap 56), passing alone and failing in company. **Named as a flake in `HELM-FEEDBACK.md` 2026-09-07 (#410 diagnose, `Expected -1 / Actual 13`) but never given a row until now** — which is why a second seat had to re-derive it. The diff under it touches the Quests tab, not Drops or the creature window; it does add a per-refresh read of the guide ledger to `QuestsView.ChecklistTickSignature`, so a timing nudge cannot be ruled out from one red and is **not** claimed to be ruled out. **Do not expand product from this red.** **AGAIN 2026-09-20, TWICE IN A ROW on PR #720** (DRA-221, DRA-216 D5), run `35491599702` — the first red and its rerun, which is what took this off the observed-rerun path: a rerun no longer cleared it and it was BLOCKING a merge. The rerun brought **a third mode**, and it is the one that proves the mechanism outright: `Assert.Equal() Failure: Expected: Heal, Actual: Unknown` at line 54 — the save had NOT landed, the file merely existed and said nothing. `SpellCatalog.Flush` writes with `File.WriteAllText`, which CREATES the file before it has written or closed it, so all three modes are one window read at three different instants: poll never exits (1), poll exits onto an open handle (2), poll exits onto an empty file (3). **Fixed as the row already prescribed**: the wait is now the store's own landed signal — a reload that reads the spell back — and the cleanup delete tolerates a lingering handle (`TryDelete`, applied to all three tests in the file, since a `Learn` arms the debounce in the other two as well). No product change: a learned spell store is re-derivable from the log, which is why it is best-effort rather than on `ProfileJson`'s atomic path (trap 65) — the flake was in the asking, not in the writing. The prove-fail the 2026-09-19 seat could not give is now committed and deterministic: `AnExistingButUnwrittenStoreIsNotALandedSave` builds the exists-but-empty state and asserts the OLD wait's exit condition is true in it while the NEW one's is false, so restoring the poll reddens. |

| `The active test run was aborted. Reason: Test host process crashed` — **no test named**, 83 of 336 passed then the host died at 2 m 45 s | 2026-09-11, local full `e2e-windows` on `claude/opus-dra45-n1` (DRA-45). The invocation that crashed was the one that also BUILT (`dotnet test` without `--no-build`); two later `--no-build` runs on the same tree passed 336/336 at 6 m 56 s and 6 m 52 s. | unknown — the abort names no test, and the 83 that had passed are not a prefix anything can grep | local Windows full suite | **open** — observed-rerun. **The one thing that distinguishes the bad run is that MSBuild was copying `EQBuddy.exe` / `EQBuddy.UI.Shared.dll` into the same `bin` the harness launches from**, which is trap 64's neighbour: the E2E project does not reference the app it launches, so a build and a launch can race over one file. Reproduced the file-lock half accidentally later the same session (`MSB3027 … locked by: EQBuddy (38512)`) by building while a run was live, which is evidence for the mechanism and not proof of this red. **Candidate disposition, not applied: `dotnet test` for this project should require `--no-build`, or `AppHarness` should assert the exe's write time is older than the run.** Not applied because it is a harness change this seat could not prove-fail without a second crash. **Do not expand product from this red** — DRA-45 adds no code the app runs before the Quests surface is opened. |
| One unnamed red, 340/341 — **test name NOT captured: this seat piped `dotnet test` through `tail -3`, the row-44 mistake repeated verbatim, four rows below the warning** | 2026-09-12, local full `e2e-windows` on `claude/fable-dra66-character-setup-20260911` (DRA-66), 7 m 1 s. Immediate full-suite rerun on the same binaries (`--no-build`, complete capture this time): 341/341 at 7 m 5 s. | unknown — nothing in the truncated output names it | local Windows full suite | **open** — observed-rerun, and the rerun is an observation. Context that narrows it without claiming it: the red run started minutes after `shoot.ps1`'s `finally` relaunched the REAL EQBuddy (installed copy, not `bin\Release`, so the screen-lock refusal does not apply to it) — a second live EQBuddy during the suite is a difference between the red run and the green one, named as a candidate and not a cause. The truncation lost even the ability to say whether the red was one of DRA-66's own touched tests — that cannot be ruled in or out, which is half of what the lost name cost; the four DRA-66-touched tests were also run as a targeted set before the full suite (4/4) and in the green rerun (341/341). **Process fix that needs no second red to prove: capture full `dotnet test` output to a file FIRST and filter the file** — this row exists because the ledger already said so once and a seat still lost the name. **Do not expand product from this red.** |
| `SpellCatalogDebouncedSaveLandsWithoutAFlush` — `debounced background save never landed` | 2026-09-12 PR #577, run `34692730242` (4307/4308). **AGAIN 2026-09-15** PR #632 (DRA-86), run `34928105133`, the only red in `build-and-test` (1/5089); the tip is a guide-DATA edit (`GuideCatalog.json` + its must-list test) that touches no store, no debounce, no spell path. **AGAIN 2026-09-19** PR #696 (DRA-212), run `35442120863`, the only red in `build-and-test` (1/5451); the tip is a **Python-only** diff (`scripts/channel-rotate.py`) that the C# suite does not load at all. **A NEW MODE on the same test:** not the 10 s wait this time — the save DID land and every assert passed, then the `finally { File.Delete(path); }` at `LearnedStoreDebounceTests.cs:56` threw `System.IO.IOException: The process cannot access the file '…eqbuddy-test-spells-debounce-47c40174….json' because it is being used by another process`. | `LearnedStoreDebounceTests.SpellCatalogDebouncedSaveLandsWithoutAFlush` | `ci/build-and-test` | **CLOSED 2026-09-20 by DRA-240** (was open — observed-rerun, three times). 10 s `File.Exists` wait for a background store write. Site-only, then guide-data-only, then Python-only — no tip under any of the three can reach the debounce, so the timing wait is the flake and not any product edit. **What 2026-09-19 ADDS is a discriminator the first two could not give:** the two modes are opposite ends of one window. `File.Exists` goes true when the debounce writer CREATES the file, not when it RELEASES it, so the poll can exit early and hand a still-open handle to the `finally` delete — mode 2 — or not exit in time at all — mode 1. That makes the candidate already named on this row the fix for BOTH, and rules out the tempting cheaper one: **lengthening the poll would not have prevented 2026-09-19**, because that run's file already existed. Candidate disposition, still NOT applied: wait on the store's own landed signal rather than polling `File.Exists`, and let the delete tolerate a lingering handle. Not applied here because the seat that saw it was landing a rotation-script card and a harness edit it cannot prove-fail is not its to guess at. **Do not expand product from this red.** |
| `DownloadsAndStagesFromGitHub` — `HttpListenerException: The process cannot access the file because it is being used by another process` from `HttpListener.Start()` | 2026-09-13, local `scripts/check.ps1` on `opus-dra71-d4` (DRA-71 D4), 4589/4590. Targeted rerun on the same binaries immediately after: 17/17. | `UpdateCheckerTests.DownloadsAndStagesFromGitHub` (`StubAssetServer..ctor`, line 226) | local `check.ps1` | **open** — observed-rerun, and the rerun is an observation. **The failure is in the test's own stub HTTP server, before any product code runs**: `StubAssetServer` binds a loopback prefix and the bind was refused, which on Windows is a port or URL-ACL collision with something else on the machine — not a claim about `UpdateChecker`. Named here rather than waved past because the ledger's rule is that a rerun does not close a row, and because the NEXT seat to see it should know the read: a listener that cannot start is a harness fact. Candidate disposition, NOT applied: the stub could retry on a fresh ephemeral port. Not applied because this seat could not prove-fail it — the collision did not recur. **Do not expand product from this red** — DRA-71 D4 touches the Helper's zone fold and no update path. |
| `OurOwnRepeatedSavesAreNotAClobber` — `System.InvalidOperationException: Collection was modified; enumeration operation may not execute` at `SettingsClobberTests.cs(66)` | 2026-09-12 PR #583 (`helm/ssc-582`), run `34724000246` | `SettingsClobberTests.OurOwnRepeatedSavesAreNotAClobber` | `ci/build-and-test` | **open** — observed-rerun only. **The tip was channel-only** (`HELM.md` + `HELM-FEEDBACK.md`), so the diff cannot be the cause. Same class as the `AForeignWriteBetweenLoadAndSaveIsReported` row above, but a NEW name gets a NEW row, not that one closed-by-rerun. The exception shape says a clobber-report list was appended while the test enumerated it — an observer-side concurrency smell, named as a candidate and not a cause. This red blocked a Helm-signed SSC land (merge-when-green); the failed job was rerun the same night this row was filed, and the rerun is an observation. **Do not expand product from this red.** |
| `TheWikiPackWindowDrawsRowsAndCarriesTheRecheck` — `the re-check button reports its target count` at `EndToEndTests.cs:215` | 2026-09-13 PR #584 (`claude/fable-exo-night3-20260913`), run `34754787753` | `EQBuddy.E2E.EndToEndTests.TheWikiPackWindowDrawsRowsAndCarriesTheRecheck` | `ci/e2e-windows` | **open** — observed once; never rerun, because the PR was closed without merge while red. **The tip was docs-only** (`HELM-FEEDBACK.md` + this ledger), so the diff cannot be the cause. Cost worth naming: this red is why #584 was not green, and the close-without-merge that followed dropped the Helm-KEEP'd `OurOwnRepeatedSavesAreNotAClobber` row above off `main` until night 4 re-landed it — a flake red on a docs tip cost a signed artifact its ride. **Do not expand product from this red.** |
| `EveryLandedRoomIsReachableByItsOwnAddress` THIRD occurrence — `address: "world:drops"`, `key: "shellWorldTab"`; **assert text NOT captured** (this seat grepped `dotnet test` for `\[FAIL\]` only, so the Expected/Actual pair was filtered before it was read — row 44's mistake again, and the reason this row cannot claim the `-1` sentinel) | 2026-09-17, local full `tests/EQBuddy.E2E` on `claude/dra149-d2-unread-worn-alias` (DRA-149 D2), 370 tests, this the only red at 8 m 20 s. Re-run of the WHOLE theory immediately after: 19/19 in 34 s. | `EQBuddy.E2E.ShellHostTests.EveryLandedRoomIsReachableByItsOwnAddress` — `address: "world:drops"` | local Windows full suite | **open** — observed-rerun, and the rerun is an observation. **Same test and same InlineData row as the 2026-09-09 half of the `shellRail` row above**, which is why it is filed against that row's signature rather than as a new mode; whether it is the same ABSENT-read could not be established here, because the assert text was not captured — capture the Expected/Actual pair next time, that is the whole discriminator. The candidate fix named on that row (one `DumpValues` read instead of two `DumpValue` moments — trap 56) is still NAMED and not applied. **Do not expand product from this red:** the DRA-149 D2 diff touches `GearUpgrades`, `Recommendations`, `HelperPresentation`, `HelperRoom`'s Helper block and the Companion Helper projection, and no World surface, no shell rail and no address. `world:drops` passed on the rerun and every other address passed in the failing run. |
| `TheShellOpensOffThePrimaryScreenWhenTheDeskHasRoomBesideIt` — a NEW name in `ShellHostTests`, not one of the three rows above; **assert text NOT captured** (this seat grepped `dotnet test` for `^\s+(Failed) EQBuddy` and the Expected/Actual pair was filtered before it was read — rows 44 and 53's mistake for the third time, which is now the ledger's most repeated process failure and wants a harness change rather than another apology) | 2026-09-19, local `tests/EQBuddy.E2E` on `sr-exec/dra217-my-classes` (DRA-216 D1), 121 tests across `QuestClassStripTests` + `QuestMyClassesTests` + `ShellHostTests`, this the only red at 4 m 14 s | `EQBuddy.E2E.ShellHostTests.TheShellOpensOffThePrimaryScreenWhenTheDeskHasRoomBesideIt` (line 110) | local Windows full suite | **open** — observed-rerun, and the rerun is an observation. **This row comes with the controlled pair the rows above mostly cannot get**, because the seat ran it before filing: (a) the test ALONE on clean `main` — green, 2 s; (b) the same 118-test company on clean `main` (the identical filter minus this branch's 3 new rows) — green, 4 m 9 s; (c) the IDENTICAL 121-test set re-run on the branch that produced the red — **green, 4 m 19 s**. So it does not reproduce on the tree that produced it, and the branch's presence is not sufficient to cause it. **Do not expand product from this red:** the DRA-216 D1 diff adds a quick-select to the quest class picker's popup (`QuestClassLens`, `ClassFilterLabel`, `EqMultiPicker.SetActions`, `QuestsView`) and touches no window placement, no shell room and no `SystemParameters` reader — and the test's own doc says it asserts a RELATIONSHIP read off `SystemParameters` at run time rather than a number, so a desk whose virtual-screen answer moved mid-suite is the shape to look at first. **Candidate, NOT applied and not proved:** unlike the `-1` rows above this one is not obviously a two-moments read, so naming `DumpValues` here would be pattern-matching rather than diagnosis; the honest next step is capturing the Expected/Actual pair, which this seat did not. |
| `EveryAddressThePickerOffersIsOneSomethingIsActuallyListeningOn` — `the picker offers <ip>:<port> and nothing answers there`; grep the assert fragment **`and nothing answers there`**, not the address, because the address is whatever NIC the runner happened to have | 2026-09-20 PR #718 (DRA-226), run `35488881871`: `10.1.0.119:59318`, the only red in `build-and-test`, 5 s (the test's own `ConnectAsync` timeout). `--failed` rerun of the SAME run: green, and `e2e-windows` green with it. | `CompanionPairingAddressTests.EveryAddressThePickerOffersIsOneSomethingIsActuallyListeningOn` (line 45) | `ci/build-and-test` | **open** — observed-rerun, and the rerun is an observation. **The test opens a real TCP connection to every NIC address the machine has**, with a 5 s timeout, and its own doc comment says it runs against “whatever NICs the machine actually has — one on CI, several on a real desk”. So a hosted runner with a SECOND NIC is the untested count, and `10.1.0.119` is a private range no assertion pinned. That makes this a harness/environment fact rather than a claim about the picker: `CompanionHost` binds every LAN address it finds, and a bind that has not finished accepting when the probe dials is the trap 62 shape — the moment an “it answers” assertion is true AT. **The tip provably cannot reach it:** PR #718 adds two PowerShell scripts, two CI steps, a `check.ps1` wiring and a CLAUDE.md trap row, with `src/` untouched — and `main` was green at this PR's exact base `46d6454d` (run at 2026-09-20T03:51) thirty minutes earlier. Candidate disposition, NOT applied: retry the connect, or wait on the listener's own accept-ready signal rather than dialling immediately. Not applied because this seat could not prove-fail it — the second NIC is the runner's, not reproducible locally, and a commit-identity card is the wrong seat to guess at a networking harness edit it cannot re-run. **Do not expand product from this red.** |
| `TheRealPublisherIsNeverCaughtHalfway` — **`the reader never ran, so this proved nothing`**. Grep that sentence, NOT `torn reads`: the two asserts are one line apart and they mean opposite things. `torn reads: N of M` is the real defect firing; this one is the anti-vacuity guard directly above it refusing to claim anything | 2026-09-20 PR #737, run `35515932572` at 14:20Z, the only red in `build-and-test` (5720 passed / 1 failed), with `e2e-windows` green beside it. **The tip provably cannot reach it: two markdown appends, no C# at all.** Cost is why this got a card rather than a rerun — it **blocked a merge for three and a half hours**, stranding a Helm LIVE ASK and a Fable release-review request on an unmerged branch (DRA-252) | `EQBuddy.Tests.WholeFilePublishTests.TheRealPublisherIsNeverCaughtHalfway` (`WholeFilePublishTests.cs:140` — the `Assert.True(reads > 0, …)` line, one ABOVE the torn-read assert) | `ci/build-and-test` | **CLOSED 2026-09-20 by DRA-257**, which landed the writer fix and this test in one pull request — so the row closes on a guard rather than on a green rerun, which is what it asked for. The closure is at the END of this cell, because the held text below is the evidence that produced DRA-257 and rewriting it would delete the reasoning. **Read the closure first if you are triaging: the candidate this row names (`File.Replace`) was MEASURED AND REFUSED.** Original disposition follows. — **open — FIXED but HELD, blocked on DRA-257, and the reason it is held is the whole value of this row.** The diagnosis is certain and the fix is written: the reader went onto `Task.Run` and the 4,000 publishes began immediately on the calling thread, so on a starved CI pool the reader task was scheduled **zero** times before `done.Cancel()` set the token, `while (!done.IsCancellationRequested)` was false on first evaluation, `reads` stayed 0, and the guard fired correctly. **That is trap 34 doing exactly its job, and the anti-vacuity line is committed unchanged.** The fix is the card's done bar: the reader SIGNALS a `ManualResetEventSlim` after its first COMPLETED read (set once, before the loop, so the signal means a read happened rather than a thread reached a line, and so it stays off the hot path), waited on with a bounded 30 s timeout **before** the publish loop, failing with its own sentence naming what did not happen; plus a dedicated thread (`TaskCreationOptions.LongRunning`) so a saturated pool cannot starve a loop that by design spins for the whole run. Prove-failed: restoring the truncating writer as the body of `WholeFilePublish.Write` reddens the SECOND assertion with **`torn reads: 128 of 215`**, `reads` 215, so the anti-vacuity line passed and the defect line fired — the fix does not measure less. **Why it is not merged: making the reader actually overlap the publisher made the REAL publisher fail.** On a 2-core `ProcessorAffinity` mask — the box shape this test's own comment names — the fixed test goes red 2 runs in 8 with `torn reads: 2566 of 77844 [empty=2566 partial=0 skipped=0 \| notFound=2566 io=0 zeroByte=2 unauth=0]`. **Every torn read is a `FileNotFoundException`: `partial=0`, `io=0`, `zeroByte=0`, `skipped=0`.** Nothing was half-written and every publish took the atomic path — the target NAME briefly did not resolve, because `File.Move(overwrite: true)` is `MoveFileEx(REPLACE_EXISTING)` and that is not atomic. `Read` catches `FileNotFoundException` under `catch (IOException)`, returns `""`, and `DumpValue` prints **`-1`** — the same sentinel the entire DRA-225 → DRA-228 family chased. **This is not a defect the fix introduced, it is one the fix stopped hiding:** the shape on `main` measured 44k–58k reads across 8 mask-3 runs with `notFound=0`, because its reader starts at an arbitrary phase; the events arrive in BURSTS (559, 1702, 2566) rather than a trickle, which is a phase-lock, not a rate. `zeroByte=2` in every run is the sibling `TheOldTruncateThenFillWriterExposesAFileWithNoKeysInIt` — the discriminator working, and the proof the two defects are distinct: truncate-then-fill reads as `zeroByte`, this reads as `notFound`, and one counter said `torn reads: N of M` for both. **So this row does NOT close on a guard that would be red 25% of the time on the CI box — that is the trap 74 shape the test's own `skipped` comment already warns about, and it would cost another 3.5 h merge exactly like the one above.** `WholeFilePublish` was scoped OUT of DRA-255 on the grounds that "there is no evidence against it here — the test never got far enough to measure it"; it got far enough, so the slice stopped and escalated rather than re-opening DRA-228's mechanism unilaterally. **DRA-257 carries the writer fix** (candidate: `File.Replace` / Win32 `ReplaceFile` when the destination exists, `File.Move` kept for the first publish onto an absent name), its 2-core prove-fail recipe, and the instruction that a 32-core run is green on the broken code and therefore cannot distinguish the hypothesis from its negation (trap 77). This row closes when DRA-257 lands and DRA-255's test merges green. **— DRA-257's CLOSURE, 2026-09-20.** The diagnosis above is confirmed by a second seat: an independent 8-run mask baseline went **3 RED of 8** at `torn reads: 951 of 77311 — absentName=951 openRefused=0 zeroByteFile=0 partialContent=0`, every torn read an absent NAME. **The named candidate is the worst of the four primitives measured, and that is the headline.** `File.Replace` / Win32 `ReplaceFile` — the one the card proposed and the one the documentation calls the atomic replace — went **12 RED of 12** at ~25% of all reads torn (`62708 of 222472 — absentName=31670 openRefused=31038`): it adds DRA-225's share-mask failure back on top of the absent name it was meant to remove. It was only possible to SEE that second failure because the counter now names `openRefused` apart from `absentName`; one number would have read as "ReplaceFile is 25% torn" with no mechanism. `File.Move` 3/8; `SetFileInformationByHandle(FileRenameInfo, ReplaceIfExists)` 1/8 at `absentName=379`; **`FileRenameInfoEx` + `FILE_RENAME_FLAG_POSIX_SEMANTICS` is what shipped** (`UI.Shared/AtomicRename`). **The window is NOT fully removable from the writer, and that is measured rather than assumed:** with the POSIX supersede, `created=0 skipped=0` — every publish took the atomic arm and nothing fell back — and the residual is bursts of **314 and 323 CONSECUTIVE** absent reads out of ~35,000, one scheduling quantum, i.e. a preemption inside the kernel's rename making an intermediate state observable. So `Read` regained a bounded retry on the exact terms its own docstring set in DRA-228 ("the evidence belongs in the flake ledger and the retry comes back WITH a test that fails without it") — both now exist. **The writer's guard is no longer this race test**, which is green on a 32-core desk against the broken publisher: it is `APublishOverALiveNameAndAPublishOntoAFreeOneAreDifferentSyscalls`, which asserts `AtomicRename.Renames` and reddens on any box. That guard exists because the first version of it did NOT: asserting `Outcome.Replaced` left a `File.Move` revert **green on all ten tests**, which is now trap 84. **— ADDENDUM, 2026-09-20, and it corrects this card's own controlled pair.** Planner reported a red `build-and-test` on PR **#740** — a two-file markdown-only change, no C# — at `torn reads: 8081 of 142003`, run `35529779603` at 18:44:53Z. **That is 5.7%, on the MAIN-SHAPE test, on the real CI box.** DRA-257's table claimed *"test as on main today: 0 red in 8 runs, `notFound=0` in ~410k reads"* and read that as the main-shape test being unable to reach the window. It is nothing of the kind: **that 0 was a property of the 2-core mask harness, not of the test.** The hosted runner starves harder than `ProcessorAffinity = 3` and reaches the window at roughly **5× the worst rate any local mask ever produced** (the baseline's worst was `951 of 77311`, 1.2%). Anyone triaging a PRE-fix red in history should expect it from the main-shape test too, and should not conclude from a green local mask run that the tree is clean — that is trap 77 one layer out, and this card wrote the weaker claim into its own evidence table. **The mode was NOT readable from that CI log, and determining it needed the tree.** PR #740's base `ae5c248a` contains neither `1cd19d8a` (DRA-255's reader handoff) nor `9ab8a2ae` (this writer fix) — both reached `main` at `8dfda1ff`, **26 minutes after** the failing run — so the message is the bare pre-instrumentation `torn reads: N of M` at `WholeFilePublishTests.cs:141`, where `torn` counts `kills < 0`, i.e. the **−1 sentinel**, which `Read` returns for ANY `IOException` **including `FileNotFoundException`**. So "torn" there is not a claim about partial content at all: those 8,081 are the absent-name window wearing a word that names a different defect. **The old counter's cost, demonstrated in production rather than in the lab** — which is exactly what done-bar item 3 split, and the reason the breakdown had to ship with the fix. #740's re-run went green and it merged at `865726bf`, still pre-fix: an observation, not a resolution. **Post-fix re-verification on this addendum:** the race test is green **8 of 8 at mask 3 and 12 of 12 at mask 1** (single core, harsher than the prove-fail bar asked for) against `3 red of 8` pre-fix, and `main` is green at `8dfda1ff`. Caveat stated rather than glossed: a green run prints no breakdown, so these 20 runs bound the failure rate and do not re-measure the residual absent-read bursts the closure above records. |
| `TheRealPublisherIsNeverCaughtHalfway` — post-DRA-257 residual, first occurrence since the closure: `torn reads: 2 of 183077 — absentName=2 openRefused=0 openDenied=0 zeroByteFile=0 partialContent=0 [created=0 skipped=0]`. Read the MODE: both torn reads are the absent-name window, every publish took the atomic arm (`created=0 skipped=0`), and the bounded reader retry did not cover two of them | 2026-09-24 PR #878 (DRA-373 D2 — `site/` + docs + landing tests; `WholeFilePublish`/`AtomicRename` untouched by the diff), run `36037751207` at 18:00Z, the only red in `build-and-test` (torn-read assert at `WholeFilePublishTests.cs:330`). **2026-09-26 PR #931** (DRA-47 `HELM.md` tip + `HELM-FEEDBACK.md` LOOP CLOSED — channel files only), run `36253171711` at 15:52Z: `torn reads: 1 of 39119 — absentName=1 openRefused=0 openDenied=0 zeroByteFile=0 partialContent=0 [created=0 skipped=0]`, same mode, same line, the only red in `build-and-test` | `EQBuddy.Tests.WholeFilePublishTests.TheRealPublisherIsNeverCaughtHalfway` | `ci/build-and-test` hosted runner | **open** — observed-rerun only. This is the residual the row above's closure records and deliberately did not re-measure ("the window is NOT fully removable from the writer"; green runs print no breakdown, so the 20 post-fix runs bounded the rate without measuring it). 2 of 183,077 (0.001%) against the pre-fix 1.2–5.7% says the fix holds and the burst mechanism can still outlast the bounded retry on a starved hosted runner. **Do not expand product from this red.** If occurrences accumulate, the measured candidate is the retry bound in `WholeFilePublish.Read` — re-measured on the mask harness, never re-litigating the primitive (the row above already measured and REFUSED `File.Replace`). |

## What a row is not

- Not a work order. Soft does not take ledger rows as inbox items.
- Not a waiver. An **open** row does not let a PR skip `e2e-windows`.
- Not resolved by “it was green for me.” Add the occurrence; leave disposition.
