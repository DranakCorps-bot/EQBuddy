# DRA-352 — For Re-Launch: chip split, Options trim, Character level/class, Mobile PoS parity

*2026-09-23 ~4:30 PM CT — Fable Planner, seat `fable-dra352`. Card: **DRA-352** (parent DRA-3
EQ-V2), Founder feedback of 2026-09-23 ~3:14 PM CT via Helm, three screenshots on the card
(`cards-windows.png`, `alerts-chips.png`, `pos-class-filter.png`) — every acceptance item below
is grounded in them. Plan only; Executor kicks after Helm SIGN. Nothing here touches Play
Console, signing, republish, release, harvest, or any store secret. No slice fetches anything.*

route: D1 hard · D2 hard · D3 hard · D4 hard · D5 hard · D6 routine

Gate: **C1 fires** (first match). D2 removes the "No longer on the widget" catalog — reversing
the Helm-signed 2026-09-05 ruling (Bevel I-11 §4, "every future HUD cut adds its row") and
narrowing the "three ways back" rule; D1 re-splits what SA-2 (2026-09-05) folded into one row;
D4 un-parks per-class levels, recorded as PARKED in `Core/CharacterLevel.cs` and `CLAUDE.md`.
All three reversals are **Founder-directed on this card's own screenshots**, which is the
evidence the walk carries. C5 also touches (guard rows retired in D2/D3) but C1 is first.
Challenger waked by card; the keyed `challenge:` line lands on this line's row before the SIGN
ask, per DRA-305 §3.1. Six-hour NO-RETURN applies from the wake stamp.

Slices are independent except D5 needs D4 (the store it writes) — land order D1‖D2‖D3‖D6 then
D4→D5. Every slice: V1-verify (targeted unit + E2E where user-visible), `WhatsNew.json` rows
land with the slice that ships the change, `docs/TestPlan.md` rows updated, shot recipes
re-staged in the same PR as the surface they photograph. One release carries all six; the
release review (Fable) runs before David is asked, unchanged.

## D1 — Split spawn vs mez chips: two rows, two inks (route: hard)

**Product rule.** Respawn chips and mez chips become **separately placeable**. Two windows:
the **Spawn row** (family `Spawn`) and the **Fight row** (families `Mez`+slow, `WatchFire`,
`Buff` — everything that is about the fight you are in stays together; the Founder asked to
separate respawn from mez, not to scatter four windows).

- `HudChipRowWindow` is parameterized by a family set + its own park pair + grow flag, two
  instances owned by `MainWindow` (`_hudChips` stays the Fight row; new `_spawnChips`).
  `HudChipRow.Build` splits its merge by the same family sets (one producer, trap 4).
- New settings `SpawnRowParkLeft/Top` (NaN = follow) and `SpawnRowGrowUp`, beside the existing
  `HudRowParkLeft/Top`/`HudChipRowGrowUp` which keep the Fight row — no migration of old
  values, and the retired `SpawnChips*`/`MezChips*` keys stay retired (this is a NEW pair, not
  their return; note it beside the AppSettings doc comment that argued the fold, citing this
  card). Both windows keep `HudDragGrip`; each `onPlaced` writes its own pair.
- **Stacking:** a slaved Spawn row follows under the Fight row — the follow placement adds the
  other slaved row's height the same way `_hudExpandBar.SlavedOccupiedHeight` is added today.
  Edit mode: the pencil opens edit on both windows; each family's Place chip appears in the
  window that owns the family; Unpark un-parks per window and says which.
- **Inks, one producer** (`HudChip.Build`): mez chips draw name + countdown in a **blue ink**,
  respawn chips in the **theme's primary text ink** (white on every dark palette — the
  Founder's ask read on his theme; Solarized gets its own legible blue/text values). The blue
  is a theme resource (`MezChipBrush`, one value per palette in the palette table), never a
  hard-coded hex in the renderer. Due state keeps `WarnBrush`; gauge rules unchanged.
- Guards: unit — family→ink table proved per family with a negative; park/placement tests for
  the second window (extend `HudParkTests`/`HudChipRowTests`). E2E — dump gains
  `spawnRowPark`/`spawnRowGrip`/`spawnRowGrow` beside the `hudRow*` keys, from one moment
  (trap 56); `scripts/drag-verify.ps1` gains the spawn-row entry (title must not collide —
  trap 24: distinct window titles, e.g. "EQBuddy Spawn Chips"). `SetChipScale`, `FocusHide`,
  `OverlayActivationTests`, `ResizableWindowTests` window lists gain the new window.
  Player copy that says "one row" (`SettingsAlertsView` spawns paragraph, `HudEditText`,
  window tooltip) is corrected in this slice. WhatsNew: "Mez chips and respawn chips are now
  two rows you place separately; mez counts down in blue, respawn in white."

## D2 — Options → Cards & windows: remove the red-boxed blocks (route: hard)

Per `cards-windows.png`. KEEP: card list with its "…is a tab in here now" sublabels
(`AbsorbedTitles` — untouched), Mini dashboard heading + checkboxes, "Double-click a HUD
chip…", "Show target drops…", Recent-rate window.

- **Remove the "No longer on the widget" block** (`SettingsHudView.BuildRetired`, both hosts —
  the Options window and the shell's Settings room render the same block). The
  `OverlaySections.Retired` data and `RetiredCardsTests` go with it — data with no surface is
  a dead store. **This reverses the 2026-09-05 Helm-signed catalog ruling by Founder
  direction (this card, 2026-09-23)**: the same PR updates the `CLAUDE.md` "three ways back"
  rule — way 1 keeps its `AbsorbedTitles` arm, the `OverlaySections.Retired` arm is retired
  with the date and this card cited, ways 2–3 unchanged. The context-menu doors themselves
  (`Guide…`, `World…`) are untouched — only the catalog paragraph goes.
- **Remove the two instructional paragraphs and the Restore-default-order button** under Mini
  dashboard (`PromotedStatsNote`, `GlancePetNote`, `_restoreOrder`). `MiniBarOrder` keeps its
  bar-drag writer (`MiniBarPresentation.SetOrder`), so `DeadSettingTests` holds; the cost —
  no one-click reset, order is undone by dragging — is accepted on the card.
- **Remove the Floating windows section — and MOVE its writer before deleting it** (trap
  26/20): the section is the ONE writer of `AppSettings.DisabledBreakouts` (measured;
  `BreakoutHost.cs` says so in its own comment). The write moves to the floating window
  itself: the ✕ `Dismissed` handler, which today writes only the in-memory intent, gains a
  persisted "stop opening on its own" arm (small toggle or the ✕ tooltip's own offer —
  executor's pick, but the WRITE persists), and `BreakoutPresentation.DismissTip` is re-worded
  to name the new door instead of the section it currently points at. Re-enabling stays
  reachable: double-click a HUD chip (kept) opens the window, and opening it clears the
  disable — a window you asked for is a window you want. `BreakoutHost`'s one-writer comment,
  `README.md:189`, and `DeadSettingTests.Known` updated to match.
- Tests that pin the removed prose are updated in the same commit, not deleted wholesale:
  `SettingsHudBlockTests` enumeration rows, `CardsTabResidualProseTests` (its word-ceiling
  test flips meaning once the notes go — re-derive the ceiling from what remains),
  `SettingsProsePolicyTests` rows naming the removed notes, `ShellHostTests`'
  `shellSettingsHudRetired`/`shellSettingsHudHints` expectations, `RetiredCardsTests`
  (deleted with its subject). `shoot.ps1` `options-cards` + `shell-settings-hud` recipes
  re-predicted; both screenshots recommitted (illustration lock). WhatsNew: names the
  removed blocks ("Options → Cards & windows is shorter: …").

**Helm LOCK — D2.2: Retired `OverlaySections` DROP** (tipped here by the D2+D3 implement PR,
DRA-355, seat `opus-dra355`, 2026-09-23). The "No longer on the widget" block's DATA goes with
its surface: `OverlaySections.RetiredCard`, `Retired`, `RetiredHeading` and `RetiredBlurb` are
deleted, not kept as an unrendered list, and `RetiredCardsTests` is deleted with its subject.
The context-menu doors (`Guide…`, `World…`) are untouched; `CLAUDE.md`'s "three ways back"
records the subtracted-card arm as retired.

**Executor note — D2.3's re-enable arm (for Helm last-look, not a re-plan).** The writer moved
to a **pin beside each floating window's ✕** (the "small toggle" option), over
`UI.Shared/BreakoutAutoOpen` — the tick list's two-halves rule lifted verbatim, so the pin and
`BreakoutHost.AutoWants` read one answer. The ✕ stays a transient close (OE-7,
`BreakoutCloseTests`). **"Opening it clears the disable" was NOT implemented**: with the pin on
the window, a summon that also wrote would make every chip PEEK a persistent edit — the
permanence OE-7 removed from the double-click — while the pin on the summoned window already
is the way back. Re-enabling stays reachable: chip opens the window, the pin re-arms it.

## D3 — Options → Alerts & chips: cut copy, hover the how-tos, one EQLWiki note (route: hard)

Per `alerts-chips.png`. All edits hit `SettingsAlertsView` and land on both hosts.

- **Remove (red):** the alert-banner drag paragraph under Alert sound; the "Used wherever
  EQBuddy speaks…" line under Alert voice; the **entire "Buff set — the missing line" block**
  (heading, paragraph, character note, picked list, class dropdown, search + popup). The buff
  set stays fully editable in the **Buffs floating window** (a complete second editor, add
  and remove — measured) and via suggestion-accept; `BuffSetsByClass` is already in
  `DeadSettingTests.Known`, so no dead store. Any prose pointing at Options for buff-set
  editing is re-pointed at the Buffs window. The alert-banner drag AFFORDANCE (the tile) is
  untouched — only the paragraph about it goes.
- **Move to hover (green), via the tab's existing ⓘ pattern** (`DesignSystem.InfoHint`,
  30 s `ToolTipPolicy`): the Track-spawns how-to paragraph → ⓘ on its checkbox row; the
  "Untick if your class never mezzes…" line → ⓘ on the Mez-countdown-chips row; the mez
  duration-logic paragraph → ⓘ on the Mez durations heading. Text moves verbatim or tighter —
  nothing new is invented (trap 73). `SettingsProsePolicy`'s body/hover split rules
  (`BodyWordCeiling`, the "too long for one hover" and "exempt paragraphs still printed"
  lists in `SettingsProsePass2Tests`) are amended to record these three as hover prose by
  Founder direction — the rule survives, the roster changes, cited to this card.
- **One EQLWiki note for mez durations:** a single line under the Mez durations heading —
  defaults are as documented on EQLWiki; type over any duration if your timers differ
  (defaults only). The per-row "as documented (eqlwiki)" notes (`MezDurationRows.Note`) drop
  the eqlwiki repetition; the measured-from-your-casts / typed-value row states remain. The
  rows, textboxes and `MezDurationsView.Commit` (the ONLY writer of `mez-overrides.json`)
  are all KEPT — this slice deletes copy, never the capability.
- `SettingsAlertsBlockTests` enumeration updated where controls leave; `ShellHostTests`'
  `alertsHints` floor re-derived (it grows). `shoot.ps1` `options-mez` +
  `shell-settings-alerts` re-predicted, screenshots recommitted. WhatsNew row.

## D4 — Character level selector + class pill, per-class level memory (route: hard)

Per `pos-class-filter.png` — the pattern is the PoS pair: a compact dropdown (the "any state"
shape) beside the multi-select pill (`EqMultiPicker`, the CLR · ENC · WAR face).

- **Character room UI** (`HomeRoom`): the Set level → type → Done flow is replaced by a
  **level dropdown** — face shows the resolved answer (default = what EQBuddy thinks, with
  its source line), list is 1–60 (a named constant with the era ladder's ceiling as its
  comment), plus the existing "Let EQBuddy work it out" clear row. Picking a level states it
  (writer below). The class chip strip is replaced by the **`EqMultiPicker`** over the same
  store (`SetStatedClasses`, cap 3 unchanged) — `MultiSelectPickerTests`' must-list gains the
  surface (trap 34's pairing), `PickerFace` owns the face words. `EQBUDDY_HOME_EDITOR` hook
  and `shellHomeStatedLevel` dump keep working (values re-keyed as needed in the same slice).
- **Per-class memory, un-parking the recorded park out loud.** `CharacterLedger` gains
  `ClassLevels` — per class, one observed and one stated `LevelReading` (the existing
  fresher-wins pair, now per class). Writers, both existing call sites re-pointed:
  - a **log ding** at level N writes observed-N to every class equipped at print time
    (`CharacterClasses.Resolve`'s answer), raise-only — a ding never lowers a class.
  - a **stated pick** of N writes stated-N to every equipped class below N, and lowers to N
    only the equipped class(es) at the current minimum (or with no memory) — so
    WAR 50 + ENC 17, stated 30 → ENC 30, WAR stays 50.
- **`ResolvedLevelFor` keeps its signature; the answer becomes the MINIMUM** over equipped
  classes' per-class resolves — the Founder's rule (WAR 50 + newly equipped 17 → the
  character is 17). Any equipped class with NO memory ⇒ that class is the minimum-unknown:
  resolve falls back to the legacy single-value pair (kept, migration-free) and the source
  line says so — unknown is never guessed (trap 73). Every reader (`TrackedLevel`, Helper
  band gates, `LevelReadout`, phone `LevelNote`) inherits through the one producer; none is
  edited. `SourceLabel`/`LevelReadout.Line` learn the "lowest of your equipped classes"
  phrasing. `CharacterLevel.cs`'s PARK comment and `CLAUDE.md`'s row are rewritten to the
  new model, citing this card — the park's premise (no log line carries per-class levels)
  stands; the source here is the player's own statement plus the equipped-set join.
- Guards: `CharacterLevelTests` + `LevelStatementStoreTests` extended — the Founder's
  WAR 50/17 example pinned by name; raise-only ding; lower-only-the-minimum statement;
  unknown-class fallback; `LedgerRoundTripTests` picks up the new field by reflection.
  E2E: `ShellHostTests` level rows re-staged. WhatsNew row.

## D5 — Phone writes classes and level (route: hard, after D4)

Today the phone changes quest-filter picks only (`classes|` verb). This slice gives the phone
the Character pair — the card's "cannot change classes or level" item.

- Two new tick verbs beside `classes|` in `CompanionActions` (quests surface, which already
  `AcceptsTicks`): `statedclasses|<list>` → `SetStatedClasses` (same name-validation and
  cap 3 as the desktop), `level|<n>` → the D4 stated-pick writer (same refusal for a
  non-positive). One writer per store — the phone calls the desktop's own methods, never a
  second resolution (trap 33). Envelope: the quests section gains nullable fields for the
  resolved level + source line and the stated classes; `CurrentProtocol` does NOT move
  (additive fields, `|| []`/null-safe on the page — `CompanionPageUpdateTests` unchanged).
- Page UI on the quests screen beside the class chips: the same dropdown + pill INTENT,
  drawn in web controls; every sentence rides the wire (trap 32), and each new caption gets
  its page-side must-list row in the same slice (the D2/D4 lesson: a sentence sent but never
  drawn passes every projection test). The Helper's read-only picks are untouched — trap 35's
  intent pattern still governs surfaces without a write verb; this surface now HAS one, and
  the `CompanionHelperSection` comment gains the sentence saying why the two differ.
- Guards: `CompanionQuestsTests` verb tests (valid, unknown-name-dropped, refused level, and
  the `ClassTapsUseTheDesktopsOwnSetClasses` pattern repeated for both verbs);
  `QuestClassLensTests`' one-writer source scan widened to admit the companion path by name;
  mobile-harness shot of the new controls staged (`mobile-harness.ps1` + snapshot fixture).
  WhatsNew row (phone-noticeable).

## D6 — Mobile PoS single-class filter parity (route: routine)

Measured gap: the page's `viewClasses()` reads the picks then `inferredClass` and **ignores
`d.characterClasses`**, so a character whose classes come from the dump or a statement gets an
unfiltered Sky list on the phone where the PC filters. Fix on the page only:

- `viewClasses()` mirrors the PC's precedence (`QuestClassLens.Offered(picks, resolved)`):
  picks, else resolved `characterClasses`, else inferred. Single-class chip taps (the lens)
  already narrow; with the fallback fixed, selecting one class filters to that class only —
  the acceptance sentence. `BuildSky` stays unfiltered on the PC side by design (#212), so
  `SurfaceParityTests`' never-narrows rows hold untouched.
- Guard: a page-behavior assertion in the harness (snapshot with stated classes, no picks —
  filtered list matches the desktop's count for that class), plus the projection tests that
  already pin `characterClasses` on the wire. WhatsNew row.

## Done bar

All six merged green on the standard bar (`build-and-test` + `e2e-windows`), each slice's
guards prove-failed where new (trap 34), WhatsNew rows in place, TestPlan rows updated,
recipes re-staged with recommitted shots, `CLAUDE.md` reversal rows updated in D2/D4's own
PRs. Then the release review (Fable) over the whole diff — before David is asked for the
re-launch go, which stays the one hard gate.
