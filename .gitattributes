# The generated catalogs are gzip streams, and CI asserts them BYTE FOR BYTE against
# their generators (`guides-transform.py --check`). Git's own binary sniffing has always
# got these right, but "has always" is not a guarantee a byte assertion should rest on:
# one line-ending conversion on checkout and the gate fails for a reason nobody changed.
*.gz binary
